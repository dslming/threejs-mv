export var TWO_PI = Math.PI * 2;
export var PI = Math.PI;
export var HALF_PI = Math.PI * 0.5;
export var QUARTER_PI = Math.PI * 0.25;
export var RADIANS = 180 / Math.PI;
export var SQRT_HALF = Math.sqrt(0.5);
